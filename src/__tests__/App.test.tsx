import React from "react";
import { render, screen } from "@testing-library/react";
import App from "../App";

describe("renders learn react link", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/Test hit/i);
  // expect(linkElement).toBeInTheDocument();
  it("sample test", () => {
    expect(1).toBe(1);
  });
});
